interface:
  display_name: "Domain Search"
  short_description: "Generate and check real-word or brandable domains"
  default_prompt: "Use $domain-search via its launcher directly, and use check --input for provided shortlists or unknown-domain follow-up with registrar links."

policy:
  allow_implicit_invocation: true
