interface:
  display_name: "Domain Search"
  short_description: "Generate and check real-word or brandable domains"
  default_prompt: "Use $domain-search to generate or check real-word and brandable domain candidates with pricing and registrar metadata."

policy:
  allow_implicit_invocation: true
