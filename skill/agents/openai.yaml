interface:
  display_name: "Domain Search"
  short_description: "Generate and check real-word or brandable domains"
  default_prompt: "Use $domain-search via its launcher directly. For open-ended domain discovery, start with search and let the built-in mixed-result trimming return a balanced shortlist of traditional .com and creative suffix domains unless the user asks for a specific style."

policy:
  allow_implicit_invocation: true
