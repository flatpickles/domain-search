interface:
  display_name: "Domain Search"
  short_description: "Generate and check real-word or brandable domains"
  default_prompt: "Use $domain-search via its launcher directly. Default to a mixed search of traditional .com plus creative suffix domains unless the user asks for a specific domain style."

policy:
  allow_implicit_invocation: true
